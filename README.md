# LINK

**安全、私密的端對端加密即時通訊系統**

LINK 是一個以安全為核心設計的即時通訊應用程式，結合 NFC 實體雙卡認證與端對端加密技術，確保只有通訊雙方能夠讀取訊息內容。

---

## 核心特色

### 🔐 NTAG 424 DNA 雙卡認證

採用 NXP NTAG 424 DNA 晶片作為實體認證媒介，實現獨特的「雙卡機制」：

| 卡片類型 | 用途 | 說明 |
|---------|------|------|
| **主卡 (Primary)** | 日常登入 | 隨身攜帶，用於一般登入操作 |
| **附卡 (Backup)** | 緊急撤銷 | 安全存放，當主卡遺失時使用附卡登入會永久撤銷主卡 |

**為什麼選擇 NTAG 424 DNA？**
- **硬體級安全**: AES-128 加密引擎，防止克隆與重放攻擊
- **SUN 動態驗證**: 每次感應產生唯一的加密簽名
- **無需電池**: 被動式 NFC，透過手機供電
- **廣泛相容**: 支援 iOS 13+ / Android 5.0+

#### NTAG 424 DNA 安全驗證流程

```
                    NTAG 424 DNA 零知識驗證

┌─────────────────┐                              ┌─────────────────┐
│   NTAG 424 DNA  │                              │     伺服器       │
│     NFC 卡片     │                              │                 │
│                 │                              │  存有同一把      │
│  內建 AES 密鑰   │                              │  AES 密鑰       │
└────────┬────────┘                              └────────┬────────┘
         │                                                │
         │  1. 用戶掃描 NFC 卡片                           │
         ▼                                                │
┌─────────────────┐                                       │
│ 卡片內部運算:     │                                       │
│                 │                                       │
│ PICC_Data =     │                                       │
│   UID + 計數器   │                                       │
│                 │                                       │
│ CMAC = AES(     │                                       │
│   密鑰,          │                                       │
│   PICC_Data     │                                       │
│ )               │                                       │
└────────┬────────┘                                       │
         │                                                │
         │  2. 產生動態 URL:                               │
         │  https://link.app/tap?                         │
         │    picc_data=EF963FF7...                       │
         │    cmac=A5B3C7D9...                            │
         │                                                │
         ▼                                                │
┌─────────────────┐         3. 傳送驗證請求               │
│   手機瀏覽器     │ ─────────────────────────────────────>│
│                 │                                       │
└─────────────────┘                                       ▼
                                                ┌─────────────────┐
                                                │ 伺服器驗證:      │
                                                │                 │
                                                │ 1. 解密 PICC    │
                                                │ 2. 檢查計數器    │
                                                │    (防重放)     │
                                                │ 3. 驗證 CMAC    │
                                                │                 │
                                                │ ✓ 確認卡片真偽  │
                                                └────────┬────────┘
                                                         │
         ┌───────────────────────────────────────────────┘
         │  4. 驗證成功，允許登入
         ▼
┌─────────────────┐
│   安全登入完成   │
│                 │
│ - 卡片已證明身份 │
│ - 密鑰從未傳輸   │
│ - 無法被複製     │
│ - 無法被重放     │
└─────────────────┘
```

**核心安全特性**:
1. **零知識證明**: 卡片證明自己擁有密鑰，但密鑰本身從不離開卡片
2. **防重放攻擊**: 每次掃描計數器 +1，舊的 URL 無法重複使用
3. **防複製**: AES 密鑰燒錄在晶片中，無法被讀取或複製
4. **雙向驗證**: 伺服器和卡片共享密鑰，互相驗證身份

### 🔒 端對端加密 (E2EE)

所有訊息在發送前於用戶端加密，伺服器只負責傳遞密文：

```
發送方                        伺服器                        接收方
  │                            │                            │
  │  1. 本地加密               │                            │
  │  ──────────────────────>   │                            │
  │                            │  2. 轉發密文                │
  │                            │  ──────────────────────>   │
  │                            │                            │  3. 本地解密
```

**加密技術棧**:
- **金鑰交換**: X25519 (Curve25519 ECDH)
- **對稱加密**: XSalsa20-Poly1305 (NaCl secretbox)
- **訊息 Padding**: 隨機填充至 64-byte 邊界，最小 256 bytes，防止長度分析攻擊

### ⚡ WebTransport 即時通訊

優先使用下一代 WebTransport 協定，提供更低延遲的通訊體驗：

| 特性 | WebTransport | WebSocket |
|------|--------------|-----------|
| 協定基礎 | HTTP/3 (QUIC) | HTTP/1.1 |
| 連線建立 | 0-RTT | 1-RTT |
| 多路復用 | 原生支援 | 需應用層實作 |
| 隊頭阻塞 | 無 | 有 |

**雙軌制設計**: 自動偵測瀏覽器支援，不支援 WebTransport 時無縫降級至 WebSocket。

---

## 技術架構

```
┌─────────────────────────────────────────────────────────────┐
│                        Frontend                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │   Svelte 5  │  │  TweetNaCl  │  │  WebTransport/WS    │  │
│  │   (Runes)   │  │  (Crypto)   │  │  (Transport)        │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ HTTPS / WSS / WebTransport
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        Backend                               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │   Go 1.22+  │  │   Fiber v2  │  │  gorilla/websocket  │  │
│  │   (Server)  │  │   (HTTP)    │  │  (Realtime)         │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │   Argon2id  │  │  JWT HS256  │  │  Circuit Breaker    │  │
│  │  (Password) │  │   (Auth)    │  │  (Resilience)       │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ pgx/v5
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     PostgreSQL 15                            │
└─────────────────────────────────────────────────────────────┘
```

### 為什麼選擇 Go？

| 優勢 | 說明 |
|------|------|
| **高併發** | Goroutine 輕量級執行緒，輕鬆處理數萬連線 |
| **編譯型** | 單一二進位檔部署，無運行時依賴 |
| **記憶體安全** | 垃圾回收 + 強型別，減少記憶體洩漏 |
| **標準庫完善** | 原生 HTTP/2、TLS、crypto 支援 |
| **交叉編譯** | 一次編譯，多平台部署 |

### 為什麼選擇 Svelte 5？

| 優勢 | 說明 |
|------|------|
| **編譯時優化** | 無虛擬 DOM，直接生成高效 DOM 操作 |
| **Bundle 極小** | 比 React/Vue 小 40-70% |
| **Runes 響應式** | `$state`、`$derived` 提供細粒度響應 |
| **零樣板** | 無需 useState、useEffect 等冗餘程式碼 |
| **原生 TypeScript** | 開箱即用的型別支援 |

---

## 快速開始

### 環境需求

- Go 1.22+
- Node.js 20+ & pnpm
- PostgreSQL 15+
- Docker (選用)

### 安裝與啟動

```bash
# 1. 克隆專案
git clone https://github.com/your-org/link.git
cd link

# 2. 啟動資料庫
docker compose up -d

# 3. 執行 Migration
cd backend && make migrate-up

# 4. 啟動後端 (開發模式)
make dev

# 5. 啟動前端 (另一個終端)
cd frontend && pnpm install && pnpm dev
```

### 測試

```bash
# 後端測試
cd backend && go test ./... -v

# 前端測試
cd frontend && pnpm test
```

---

## 專案結構

```
link/
├── backend/
│   ├── cmd/server/          # 應用程式入口
│   ├── internal/
│   │   ├── config/          # 設定管理
│   │   ├── domain/          # 領域模型 & 介面
│   │   ├── repository/      # 資料存取層
│   │   ├── service/         # 業務邏輯層
│   │   ├── handler/         # HTTP 處理器
│   │   ├── middleware/      # 中介軟體
│   │   ├── transport/       # WebSocket/WebTransport
│   │   └── pkg/             # 共用工具
│   └── migrations/          # 資料庫遷移
├── frontend/
│   ├── src/
│   │   ├── lib/
│   │   │   ├── api/         # API 客戶端
│   │   │   ├── crypto/      # 加密模組
│   │   │   ├── stores/      # Svelte Stores
│   │   │   ├── transport/   # 傳輸層
│   │   │   └── components/  # UI 元件
│   │   └── routes/          # 頁面路由
│   └── static/
├── certs/                   # TLS 證書 (本地開發)
├── note/                    # 開發筆記
└── docker-compose.yml
```

---

## 安全設計

### 密碼儲存
- **Argon2id** (OWASP 推薦參數)
- Memory: 64 MB, Iterations: 3, Parallelism: CPU cores

### JWT 防護
- **演算法白名單**: 僅接受 HS256，拒絕 none 演算法攻擊
- **短效期**: Access Token 15 分鐘

### 傳輸安全
- **TLS 1.3**: 所有連線強制加密
- **Rate Limiting**: 登入/註冊端點限速

### 私鑰保護
- 私鑰儲存於瀏覽器 IndexedDB
- 使用 PBKDF2 從密碼衍生加密金鑰
- 首次登入時顯示安全警告

---

## 開發狀態

- [x] Phase 1-2: 環境建置 & 基礎建設
- [x] Phase 3-5: 後端 Domain / Repository / Service
- [x] Phase 6: Transport 雙軌制
- [x] Phase 7: 前端加密 + 雙卡 UI
- [x] Phase 8: 前端 Transport + 聊天
- [ ] Phase 9: E2E 測試

---

## 授權

MIT License
