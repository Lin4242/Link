# Seed Script Configuration
# Copy this file to .env.seed and fill in your values
# Then run: source .env.seed && go run ./cmd/seed

# Database connection (use production URL for production seed)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/link?sslmode=disable

# Demo user configuration
SEED_DEMO_NICKNAME=小安
SEED_DEMO_PASSWORD=demo1234

# NFC Card tokens (IMPORTANT: these must match the tokens burned to your physical NFC cards)
# You can find these tokens in the admin panel after generating a card pair
SEED_DEMO_PRIMARY_TOKEN=your-primary-token-here
SEED_DEMO_BACKUP_TOKEN=your-backup-token-here
